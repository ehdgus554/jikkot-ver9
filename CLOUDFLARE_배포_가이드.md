# 직꼿 VER9 브라우저 배포 가이드

이 방식은 컴퓨터에 Node.js를 설치하거나 명령어를 입력하지 않습니다. GitHub와 Cloudflare 계정만 사용합니다.

## 1. Deploy to Cloudflare 버튼 누르기

저장소 첫 화면의 `Deploy to Cloudflare` 버튼을 누릅니다.

## 2. GitHub 계정 연결하기

Cloudflare가 GitHub 로그인을 요청하면 직꼿 저장소를 만들 GitHub 계정으로 로그인하고 권한을 승인합니다.

## 3. 새 저장소와 Worker 이름 확인하기

Cloudflare 설정 화면에서 다음 항목을 확인합니다.

- GitHub 저장소 이름: 기본값을 사용하거나 `jikkot-ver9` 입력
- Worker 이름: 기본값을 사용하거나 `jikkot-ver9` 입력
- D1 데이터베이스 이름: `jikkot-ver9-db`

## 4. 배포 시작하기

화면의 `Deploy` 버튼을 누릅니다. Cloudflare가 다음 작업을 자동으로 진행합니다.

1. GitHub 계정에 직꼿 저장소 복사
2. D1 데이터베이스 생성 및 연결
3. 회원·세션·비회원 이용 기록 테이블 생성
4. 직꼿 VER9 빌드 및 Worker 배포

완료될 때까지 화면을 닫지 않습니다. 첫 배포는 몇 분 정도 걸릴 수 있습니다.

## 5. 배포 주소 열기

배포가 완료되면 화면에 표시되는 `workers.dev` 주소를 엽니다.

다음 항목을 확인합니다.

- 회원가입 후 로그아웃·재로그인 가능
- 비회원으로 첫 루틴 1개 열람 가능
- 같은 날 두 번째 루틴을 열면 로그인 팝업 표시
- 회원 로그인 후 전체 동작 후보 열람 가능

## 이후 업데이트

새 코드가 GitHub 저장소의 기본 브랜치에 반영되면 Cloudflare가 자동으로 다시 빌드하고 배포합니다. 기존 D1 연결을 유지하면 회원정보와 이용 기록도 유지됩니다.

## 주의사항

- 공개 GitHub 저장소에는 프로그램 코드만 들어 있습니다. 실제 회원정보와 비밀번호는 포함되지 않습니다.
- 회원 데이터는 배포 과정에서 본인의 Cloudflare 계정에 새로 생성되는 D1에 저장됩니다.
- GitHub 저장소를 삭제해도 이미 만들어진 D1이 자동으로 삭제되는 것은 아닙니다.
